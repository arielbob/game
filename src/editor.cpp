#include "editor.h"

void pick_entity(Game_State *game_state, Vec2 cursor_pos) {
    Editor_State *editor_state = &game_state->editor_state;
    Mesh_Table

    Entity *entities = game_state->entities;
    for (int32 i = 0; i < game_state->num_entites; i++) {
        Entity e = entities[i];
        
    }
}
